name: Apply Codex UI tweaks

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/codex-ui-tweaks.yml
      - scripts/codex-ui-tweaks.mjs

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply UI tweaks
        run: node scripts/codex-ui-tweaks.mjs
      - name: Commit UI tweaks
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add frontend/src/App.jsx frontend/src/styles.css
          if git diff --cached --quiet; then
            echo "No UI changes to commit"
          else
            git commit -m "Refine chat layout and sticker picker"
            git push
          fi
